//conditional operator
//input an int.wap that check whether the no.is +ve,-ve or zero
void main()
{
	int a;
	clrscr();
	printf("Enter a no.");
	scanf("%d",&a);
	a>0?printf("%d is positive",a): a<0?printf("")
}